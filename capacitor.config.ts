import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Taschenrechner-App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
